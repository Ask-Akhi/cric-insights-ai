# Web framework
fastapi>=0.115.5
uvicorn[standard]>=0.30.1
pydantic>=2.7.4
httpx>=0.28.1

# Data — polars bundles its own arrow; no need for separate pyarrow
polars>=1.14.0

# LLM — direct clients (lazy-imported inside route handlers, not at startup)
google-genai>=1.66.0
openai>=1.40.0

# LangChain + LangGraph — imported lazily inside cricket_graph.py only
langchain>=0.2.0
langchain-google-genai>=1.0.7
langchain-openai>=0.1.0
langchain-core>=0.2.0
langgraph>=0.1.0

# Config
python-dotenv==1.0.0
